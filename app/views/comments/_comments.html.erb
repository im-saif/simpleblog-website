<h3> Comments </h3>
<% @post.comments.each do |comment| %>
    <div class = 'well'>
        <p> <strong> <%= comment.username %> </strong>: <%= comment.body %> 
        <% if user_signed_in? %>
         <%= link_to '❌', [comment.post, comment], method: :delete, data: {confirm: 'Are you sure?'} %>  </p>
        <% end %>
    </div>
<% end %>
